---
title: Slider
description: A sleek media progress slider with chapter toggle, draggable thumb, and time display.
category: Components
---

The Slider component is a media progress control inspired by video player timelines. It features a draggable thumb, real-time time display, and an optional chapter toggle.

## Preview

<ComponentPreview name="slider" />

## Usage

### Installation

<InstallBlock command="slider" />

### Basic Example

```tsx
"use client";

import { Slider } from "@/registry/klarden-ui/slider";
import { useState } from "react";

export default function Example() {
  const [value, setValue] = useState(122);
  const max = 5363;

  return (
    <Slider
      value={value}
      max={max}
      onValueChange={setValue}
      onValueCommit={setValue}
    />
  );
}
```

## Properties

<PropsTable>
  <Prop
    name="value"
    type="number"
    default="—"
    description="Current progress value in seconds. Required."
  />
  <Prop
    name="max"
    type="number"
    default="—"
    description="Maximum value (total duration) in seconds. Required."
  />
  <Prop
    name="onValueChange"
    type="(value: number) => void"
    default="—"
    description="Callback when value changes during drag."
  />
  <Prop
    name="onValueCommit"
    type="(value: number) => void"
    default="—"
    description="Callback when dragging ends (mouseup)."
  />
  <Prop
    name="color"
    type="SliderColor"
    default='"default"'
    description="Color variant for the track fill and thumb. Options: default, blue, purple, pink, red, orange, green, teal, cyan, indigo, violet, rose, amber, lime, sky, emerald, fuchsia."
  />
  <Prop
    name="thumb"
    type='"circle" | "square" | "diamond" | "rounded" | "line"'
    default='"circle"'
    description="Shape variant for the slider thumb."
  />
  <Prop
    name="showRemaining"
    type="boolean"
    default="true"
    description='Show remaining time as negative (true) or total duration as positive (false).'
  />
  <Prop
    name="disabled"
    type="boolean"
    default="false"
    description="Disables all interactions."
  />
  <Prop
    name="className"
    type="string"
    default="—"
    description="Additional CSS classes for the container."
  />
</PropsTable>
